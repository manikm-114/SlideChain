MATLAB has toolbox commands that implement projection, filtering, and filtered backprojection. 

The keyword is radon. You supply an image and theta. Theta can be a single value, which gives one projection, or an array, which gives many projections.